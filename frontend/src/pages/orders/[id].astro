---
import { OrdersApp } from "../../components/OrdersApp";
import Layout from "../../layouts/Layout.astro";

export const prerender = false;
const { id } = Astro.params;
---
<Layout title={`Order ${id} — Nexora`}>
  <OrdersApp orderId={id} client:load />
</Layout>
